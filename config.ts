
/**
 * Application configuration.
 * Translation and News Search are now powered exclusively by the Gemini API
 * using the system-provided process.env.API_KEY.
 */
export const CONFIG = {
  APP: {
    NAME: 'AI News Pulse',
    VERSION: '2.2'
  }
};
